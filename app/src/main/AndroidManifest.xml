<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<<<<<<< HEAD
    <!-- Networking -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Background service (Porcupine needs this on some devices) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
=======
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
>>>>>>> ec9c58831b83e21122cc7affc0a4e19d1e7c7676

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/audibot_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/audibot_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AudiBot">
<<<<<<< HEAD

        <!-- Launcher: LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Hey Shift"
            android:theme="@style/Theme.AudiBot"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.AudiBot"
            android:windowSoftInputMode="stateHidden|adjustPan"/>

        <!--
            FIX: Added android:launchMode="singleTop" so that onNewIntent() fires
            correctly when SessionDetailActivity navigates back to MainActivity
            with resume_session_id. Without singleTop the OS creates a new instance
            and the resume logic never runs.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="Hey Shift"
            android:launchMode="singleTop"
            android:theme="@style/Theme.App.Starting"
            android:windowSoftInputMode="stateHidden|adjustResize"/>

        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:label="Profile"
            android:theme="@style/Theme.AudiBot"
            android:windowSoftInputMode="stateHidden|adjustPan"/>

        <activity
            android:name=".ChatHistoryActivity"
            android:exported="false"
            android:label="Chat History"
            android:theme="@style/Theme.AudiBot"
            android:windowSoftInputMode="stateHidden|adjustPan"/>

        <!--
            FIX: Added android:launchMode="singleTop" so that FLAG_ACTIVITY_SINGLE_TOP
            in the Intent from ChatHistoryActivity actually hits onNewIntent() instead
            of creating a duplicate stack entry.
        -->
        <activity
            android:name=".SessionDetailActivity"
            android:exported="false"
            android:label="Chat"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AudiBot"
            android:windowSoftInputMode="stateHidden|adjustResize"/>

=======
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Hey Shift"
            android:theme="@style/Theme.App.Starting"
            android:windowSoftInputMode="adjustResize"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
>>>>>>> ec9c58831b83e21122cc7affc0a4e19d1e7c7676
    </application>

</manifest>